{{#each domains}}
    <div class="mdl-cell mdl-cell--12-col">
        <h2>{{domain}}</h2>
        {{#each data}}
            {{> showResult record=this}}
        {{else}}
            No record
        {{/each}}
    </div>
{{/each}}